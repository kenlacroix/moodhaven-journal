# npm audit exceptions
# GHSA-67mh-4wv8-2f99: esbuild <=0.24.2 dev-server CORS issue
# Affects vite/vitest (dev-only tooling). The fix requires vite@8 which is a
# breaking change. Risk is limited to local development servers; the shipped
# Tauri binary does not include esbuild/vite at runtime.
# Re-evaluate when @vitejs/vite 7.x ships a non-breaking esbuild bump.
audit-level=high
