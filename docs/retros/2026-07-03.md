# Weekly Retro — 2026-07-03

## What shipped

No PRs were merged and no commits landed on `main` this week (2026-06-26 → 2026-07-03). The previous week closed strongly (six PRs merged on 2026-06-24 including the quinn-proto security fix), but this week was a full stop on feature work.

**Security housekeeping (automated, not yet merged):**
- `anyhow` 1.0.102 → 1.0.103 patched by nightly advisory scan (RUSTSEC-2026-0190 — `Error::downcast_mut()` unsoundness, published 2026-06-25). PR #240 is open, not merged.

## In progress

| PR | Title | Status |
|:---|:---|:---|
| #240 | `chore(deps)`: nightly advisory scan — 1 advisory closed, 2 surfaced | Open (needs merge) |
| #239 | `chore(ci)`: bump `actions/cache` 5.0.5 → 6.1.0 (Dependabot) | Open (needs merge) |
| #238 | `chore(ci)`: bump `actions/setup-java` 5.3.0 → 5.4.0 (Dependabot) | Open (needs merge) |
| #237 | `chore(retro)`: weekly engineering retro 2026-06-26 | Open — fell back to `docs/retros/` (artifacts repo out of scope last session) |

Three open security issues are also active:
- **#241** (opened today): Two new `quick-xml` high-severity advisories — RUSTSEC-2026-0194 (quadratic run time on duplicate-attribute check) and RUSTSEC-2026-0195 (unbounded namespace-declaration allocation — memory exhaustion DoS). Both CVSS 7.5. Transitive via `tauri-winrt-notification` and `plist`; upstream patches not yet available.
- **#236** (2026-06-26): `cargo audit` was blocked by HTTP 403 from proxy — one-day monitoring gap. `form-data` npm advisory also flagged again (see Concerns).

## Wins

- **Automated patching working**: The nightly advisory scan autonomously detected, patched, and opened a PR for the `anyhow` unsoundness (RUSTSEC-2026-0190) within one day of the advisory being published — same-day response with zero manual intervention needed.
- **quinn-proto fully closed**: RUSTSEC-2026-0185 (memory exhaustion, CVSS 7.5) was fixed last week (#233, merged 2026-06-24) and issue #231 is closed. Cargo.lock now shows `quinn-proto 0.11.15`.
- **Security instrumentation is comprehensive**: The `quick-xml` advisories were caught and filed as #241 within hours of publication, with full dependency chain analysis.

## Concerns / blockers

- **Zero feature velocity this week.** No commits, no merges. Whether this is deliberate (planned break, focus shift) or a stall is unclear — worth a check-in.
- **PR #237 (last week's retro) unmerged.** It fell back to `docs/retros/` inside `moodhaven-journal` because the artifacts repo token was out of scope. Both the routing issue (token scope) and the unmerged PR need attention. This week's retro has the same routing limitation.
- **`form-data` npm advisory unresolved for 14+ days.** Has appeared in daily security scans since 2026-06-19 (issues #201, #204, #210, #235, #236). It's a dev-only transitive dep (via `jsdom` → vitest), not present in the shipped bundle, but it either needs a fix (`npm audit fix` or an `overrides` entry in `package.json`) or a documented acceptance decision to stop the noise.
- **`quick-xml` advisories blocked on third parties.** RUSTSEC-2026-0194 and -0195 require `tauri-winrt-notification` and `plist` to release versions that pull in `quick-xml >= 0.41.0`. Nothing actionable until upstream ships. Tracked in issue #241.
- **`cargo audit` proxy block (2026-06-26).** The advisory database fetch returned HTTP 403 for one day (issue #236). The monitoring gap was silent rather than a hard failure — worth considering whether a blocked audit should be treated as a blocking CI error.

## Next week

- **Merge the open dependency/CI PRs**: #238, #239, #240 are low-risk routine bumps — clear the queue.
- **Resolve or formally close the `form-data` advisory**: Either add an `overrides` entry in `package.json` or document accepted risk and close the issue family once and for all.
- **Monitor `quick-xml` upstream releases**: When `tauri-winrt-notification` or `plist` ship with `quick-xml >= 0.41.0`, update and close #241.
- **Resume feature work**: Identify what stalled this week and unblock it. The project was shipping regularly (v1.8.0, v1.9.0, Android Phase 2) — a full-week gap is noticeable.
- **Fix retro routing**: Ensure the weekly retro agent has `gstack-artifacts-ken` in scope so retros land in the right repo instead of falling back to `docs/retros/` here.

---
*Auto-generated by weekly retro agent — 2026-07-03*
