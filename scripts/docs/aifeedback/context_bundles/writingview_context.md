# Context Bundle: Writingview
_Generated by ai_code_scan.py — paste into Claude for implementation_

---

## Task

~~~markdown
# WritingView Responsive Layout — Context

## Current State

* The `WritingView` displays the main journal/editor area in MoodBloom.
* It does not scale well on lower screen dimensions.
* On small windows or devices, content is often cut off or requires scrolling.
* Layout is currently static with fixed padding and margins.
* Expansion/collapse behavior is limited or absent.

## Desired Improvements

1. **Responsive Layout**

   * `WritingView` should automatically expand to fill available space.
   * Should collapse or adjust margins/padding when vertical or horizontal space is limited.
   * Maintain usability across:

     * Desktop (wide screens)
     * Smaller laptops
     * Tablets
     * Mobile (we already have a comprehensive mobile UI)

2. **Dynamic Content Display**

   * Maximize visible content without requiring excessive scrolling.
   * Toolbar, tags, and extra panels should adjust:

     * Collapse or hide optional panels when space is limited.
     * Resize text area dynamically.

3. **Flexibility & Adaptation**

   * Support window resizing in real time.
   * Support different aspect ratios (wide, square, vertical).

4. **UX Considerations**

   * Keep key actions visible (save, undo, formatting).
   * Avoid cutting off content or controls.
   * Smooth animations when expanding/collapsing panels.

5. **Implementation Notes**

   * Consider using a combination of CSS flexbox/grid and dynamic component sizing.
   * Ensure state of collapsed/expanded panels persists when returning to `WritingView`.
   * Test across multiple screen sizes to ensure proper behavior.

## Goal

* `WritingView` becomes fluid and adaptive.
* Users see as much content as possible, regardless of window or device size.
* Maintain usability, readability, and access to core editing tools.
~~~

## Expected Behaviour

_Not specified_

---

## Bug Analysis (GPT)

### 1. Likely Root Cause
The issue in the **WritingView** component appears to stem from the `scheduleAutoSave` function in `/home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/WritingView.tsx`. The condition checking for save criteria may not be properly accounting for previous states or entries, leading to potential unwanted behavior during the auto-save sequence.

The relevant section:
```javascript
if (!contentText.trim()) return;
if (!savedEntryIdRef.current && wordCount < 5) return;
```
This suggests that the auto-save won't activate if there’s not enough text, which may be too restrictive for new entries or specific user behaviors.

### 2. Where in the Code
The change needs to be made in the `/home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/WritingView.tsx` file, specifically within the `scheduleAutoSave` function.

#### Current Behavior:
- The function may prevent saving if content text is empty or if the word count is less than 5 when it's a new entry.
  
#### Why It's Wrong:
- This logic could lead to a scenario where users might have written meaningful content that qualifies for saving but doesn't meet the word count requirement, particularly when the editor is new and the user starts to type.

### 3. Suggested Fix
1. Navigate to the `/home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/WritingView.tsx` file.
2. In the `scheduleAutoSave` function, modify the following line:
   ```javascript
   if (!savedEntryIdRef.current && wordCount < 5) return;
   ```
   Change it to allow saving under certain conditions:
   ```javascript
   if (!savedEntryIdRef.current && (wordCount < 5 && !contentText.trim())) return;
   ```
3. This change would allow for a manual trigger of the save process even when the contentText does not exceed five words at the initial typing phase, ensuring that users who are starting to write still have their progress captured.

### 4. Files to Change
| File                                                    | Function / area         | What to change                                                  |
|---------------------------------------------------------|-------------------------|----------------------------------------------------------------|
| /home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/WritingView.tsx | scheduleAutoSave function | Modify the condition to allow save when word count < 5 but not empty |

### 5. How to Verify the Fix
1. Open the **WritingView** component in the application.
2. Begin a new journal entry and type less than 5 words, ensuring the auto-save should now activate.
3. Simulate a real use case:
   - Write a short entry, such as “Just starting here…” and stop typing for more than 2 seconds.
   - Check if an auto-save occurs.
4. Confirm that after the entry is created, the content is appropriately saved and visible in any relevant journal listing or entry display. 
5. Repeat the test with existing entries where the content exceeds 5 words just to ensure no regressions affect currently functioning components. 

By following these steps, the bug's resolution can be validated, checking that the auto-save functionality is enhanced without introducing new issues.

---

## Files to Read

_Slim mode — read these files directly before implementing._

- `/home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/JournalPage.tsx` (~965 tokens)
- `/home/ken/Documents/GitHub/MoodBloom-Tauri/src/pages/WritingView.tsx` (~7,917 tokens)

_Total: 2 files_
