<?xml version="1.0" encoding="utf-8"?>
<!--
  Mood wheel item. The colored pill background is set programmatically in MoodAdapter
  (semi-transparent when off-center, opaque when focused/tapped).
  WearableLinearLayoutManager.LayoutCallback scales and fades items near the edges,
  creating a natural wheel/drum-roll effect on round watch faces.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- Pill background — color and corner radius set in MoodAdapter -->
    <View
        android:id="@+id/colorPill"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/mood_pill" />

    <!-- Content row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="14dp">

        <!-- Emoji (large) -->
        <TextView
            android:id="@+id/moodEmoji"
            android:layout_width="40dp"
            android:layout_height="wrap_content"
            android:textSize="28sp"
            android:gravity="center" />

        <!-- Label + sublabel column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/moodLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="17sp"
                android:textColor="@color/wear_text_primary"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Mood level number (right side) -->
        <TextView
            android:id="@+id/moodLevel"
            android:layout_width="20dp"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/wear_text_primary_alpha53"
            android:gravity="center" />

    </LinearLayout>

</FrameLayout>
