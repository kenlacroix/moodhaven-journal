// StillHaven — somatic companion module entry view
// Phase 0: stub — renders null until Phase 4 fills in the check-in UI.
// Gated on VITE_FEATURE_STILL in App.tsx.

export function StillView(): null {
  return null;
}
